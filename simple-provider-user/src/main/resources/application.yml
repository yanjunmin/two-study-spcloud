server:
  port: 8000
spring:
  application:
    name: user
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: h2
    schema: classpath:schema.sql
    data: classpath:data.sql

logging:
  config: classpath:logback-spring.xml
#logging:
#  level: info
